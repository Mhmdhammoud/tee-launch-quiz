import {UtilActions} from './UtilTypes';


// Here we can add all action interfaces with the | operator and we are exporting AppActions as a main interface for the whole project
// This structure can be optimized by splitting actions for mutiple role systems
export type AppActions = UtilActions;